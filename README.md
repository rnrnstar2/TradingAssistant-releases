# TradingAssistant-releases

Public release assets for Trading Assistant Desktop.

This repository hosts the installer, updater manifests (`latest.json`), signed update bundles, and EA packages.

| Tag | Contents |
|-----|----------|
| `latest-desktop` | Desktop app installer, updater bundle, `latest.json` manifest, EA zip |